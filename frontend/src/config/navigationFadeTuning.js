/**
 * GLOBAL PAGE NAVIGATION FADE — SIMPLE MANUAL CONTROL
 *
 * Presentation only. Navigation still happens immediately and no game state
 * or route timing is changed.
 */
export const NAVIGATION_FADE_TUNING = Object.freeze({
  durationMs: 290,
  startOpacity: 0.58,
  moveY: 3,
  startScale: 1,
});
