/**
 * CALENDAR GAME-TILE LOGO — MANUAL VS CODE CONTROL
 *
 * ONLY CHANGE THESE FOUR NUMBERS.
 *
 * x: positive = right, negative = left
 * y: positive = down,  negative = up
 * scale: 1 = normal, 1.20 = 20% larger, 0.80 = 20% smaller
 * opacity: 1 = fully visible, 0.70 = 70% visible
 *
 * x/y and the base logo size automatically scale proportionally for normal
 * desktop/laptop widths. Oversized logos are clipped by the calendar tile.
 */
export const CALENDAR_TILE_LOGO_TUNING = Object.freeze({
  x: -30,
  y: 0,
  scale: 2.8,
  opacity: 0.8,
});
