BM Patch 38 - Team Intel + Locker Room Realism

Scope:
- Team Intel lineup now uses saved/smart rotation order and minutes instead of greedy OVR-only position fill.
- Team Intel protects contender cores/key role fits internally so top-six players do not leak to trade block.
- Trade block now requires a real availability reason; salary alone is not enough.
- Target search now requires both fit for your team and source-team availability.
- Target frequency cap persists and now respects per-player maxTargetUses.
- Locker Room interprets deflated OVRs through old-version role pressure bands and adds actionTags.

Not touched: trade acceptance/execution, CPU trade bank, FA signing logic, sim engine, player ratings JSON.
